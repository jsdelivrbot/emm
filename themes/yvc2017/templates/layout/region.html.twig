{#
/**
 * @file
 * Theme override to display a region.
 */
#}

{# DevCollab added custom regions  #}
{%
  set classes, constrain = [
    'region-' ~ region|clean_class,
  ],
  [
    'page_top',
    'breadcrumb',
    'help',
    'preface',
    'highlighted',
    'intro',
    'hero',
    'postscript',
    'footer',
    'footer_bottom',
    'page_bottom'
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {% if region in constrain %}<div class="l-constrain clearfix">{% endif %}
      {{ content }}
    {% if region in constrain %}</div>{% endif %}
  </div>
{% endif %}
